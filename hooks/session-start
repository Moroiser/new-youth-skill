#!/bin/bash
# Session start hook for new-youth-skill
# This hook is called when a new session starts

# Read the skill content and inject as system context
SKILL_PATH="$(dirname "$0")/../SKILL.md"

if [ -f "$SKILL_PATH" ]; then
    # Skill is available for context injection
    # The actual injection is handled by the agent platform
    exit 0
else
    exit 1
fi
